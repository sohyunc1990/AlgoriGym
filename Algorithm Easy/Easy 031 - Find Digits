function findDigits(n) {
    let numberInString = n.toString(),
        answer = 0;
        
    for(let i=0; i<numberInString.length; i++){
        if(n%numberInString[i] == 0){
            answer++
        }
    }
    return answer
}
